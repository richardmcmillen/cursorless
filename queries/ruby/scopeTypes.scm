(comment) @comment

(method) @function

[
 (string)
 (bare_string)
 (subshell)
 (heredoc_body)
] @string

(class) @class

(hash
  (pair
    key: (_) @key)) @map

(hash
  (pair
    value: (_) @value)) @map